# Q-1. Implement singleton class

# Q-2 Implement builder pattern

# Q-3 Find the second-largest element in an array without sorting

# Q-4 Given two strings `s1` and `s2`, swap them without using a third variable.

# Q-5 Given a string, find the freq of character using streams

# Q-6 Find the freq map of vowels in a string

# Q-7 Given an array of strings, find the longest string in it using streams.

# Q-8 Find the repeating and missing numbers from the array of numbers.

# Q-9 Given an array of numbers. Find the largest product we can obtain from multiplying two elements in the arrry. For, example:

```text
Input: [3, 6, 2, 8, 4, 5] Output: 48
(which is the maximum product of 6 and 8)
```

# Q-10 Write a program to find the nth prime number (it should work upto 1 millionth prime)